{
  "name": "klirbuild-desktop",
  "version": "0.1.0",
  "private": true,
  "description": "KlirBuild Windows / desktop shell (Tauri) → https://klirline.app",
  "scripts": {
    "tauri": "tauri",
    "dev": "tauri dev",
    "build": "tauri build",
    "build:windows": "tauri build --target x86_64-pc-windows-msvc"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2.5.0"
  }
}
